First download all files in the repository into the same directory.

Install required Python libraries: 

 - pip install numpy

 - pip install scipy

 - pip install matplotlib

 - pip install torch

 - pip install torchvision

 - pip install pandas

Run the CNN training script:
 - python train_cnn.py

After training completes, run the hybrid simulation:
 - python main.py
